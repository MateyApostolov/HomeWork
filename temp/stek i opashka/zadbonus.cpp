#include <bits/stdc++.h>
using namespace std;

int main () {
    int n;
    cin >> n;
    stack <int> normal;
    stack <int> obr;
    while(n > 0)
        normal.push(n % 2);
        n /= 2;
    }
    return 0;
}
